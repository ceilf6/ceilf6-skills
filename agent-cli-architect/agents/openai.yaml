interface:
  display_name: "Agent CLI Architect"
  short_description: "Shape the entry layer of agent CLIs"
  default_prompt: "Use $agent-cli-architect to classify this tool's entry paths, collapse duplicate boot flow, and separate CLI subcommands from REPL commands."
