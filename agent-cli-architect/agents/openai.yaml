interface:
  display_name: "Agent CLI Architect"
  short_description: "Architect converged multi-mode agent CLIs"
  default_prompt: "Use $agent-cli-architect to map this CLI's entry paths, converge startup flow, and redesign command registration."
