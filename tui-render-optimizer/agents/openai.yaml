interface:
  display_name: "TUI Render Optimizer"
  short_description: "Optimize React TUI rendering pipelines"
  default_prompt: "Use $tui-render-optimizer to locate the bottleneck in this terminal render pipeline and propose targeted fixes."
