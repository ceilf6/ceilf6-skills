interface:
  display_name: "TUI Render Optimizer"
  short_description: "Tune the terminal render layer"
  default_prompt: "Use $tui-render-optimizer to isolate whether this TUI issue lives in commit, layout, diff, or terminal I/O and propose targeted fixes."
