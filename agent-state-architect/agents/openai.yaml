interface:
  display_name: "Agent State Architect"
  short_description: "Tier state for streaming agent interfaces"
  default_prompt: "Use $agent-state-architect to classify this agent UI state by tier, centralize side effects, and reduce streaming re-renders."
