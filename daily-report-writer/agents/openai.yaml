interface:
  display_name: "Daily Report Writer"
  short_description: "Create evidence-backed KM daily reports"
  default_prompt: "Use $daily-report-writer to collect today's work from KM, code commits, PRs, ONES, TT, and calendar, then create my daily report in the configured Citadel directory."
