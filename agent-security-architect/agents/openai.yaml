interface:
  display_name: "Agent Security Architect"
  short_description: "Design agent permission and safety systems"
  default_prompt: "Use $agent-security-architect to map this agent's threat model, permission pipeline, approval channels, and mode transitions, then propose a fail-closed security architecture."
